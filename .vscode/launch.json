{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: index",
            "request": "launch",
            "command": "yarn run index",
            "cwd": "${workspaceFolder}"
        }
    ]
}